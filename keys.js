const glass = {
     name: 'glass',
     color : 'red',
     price : 250,
     isCleand : true
}
// console.log(glass);

const keys = Object.keys(glass);
// [ 'name', 'color', 'price', 'isCleand' ] output of keys

// console.log(keys)

const values = Object.values(glass);
// [ 'glass', 'red', 250, true ] output of values 
console.log(values)